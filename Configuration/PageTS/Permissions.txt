// Permissions for Fachreferenten
[usergroup = 1]

    // hide unwanted modules and submodules
    options.hideModules = view, func help, tools, system
    options.hideModules.web := addToList(ViewpageView,func,ts,RealurlRealurl)

   // hide unwanted selection of doctypes for page
	TCEFORM.pages.doktype.disabled = 1
	// hide unwanted doctypes in "new record wizzard"
	mod.web_list.deniedNewTables = tx_news_domain_model_tag,tx_devlog,sys_domain,tx_rtehtmlarea_acronym,sys_template,tx_scheduler_task_group,sys_note,sys_file_collection
    // show wizzard in list form (few elements anyway)
    // mod.wizards.newContentElement.renderMode = list
    // feature has been removed in 7.6.11

	// hide unwanted real-url specific information for page
	TCEFORM {
	    pages.tx_realurl_exclude.disabled = 1
	    pages.tx_realurl_pathsegment.disabled = 1
	    pages.tx_realurl_pathoverride.disabled = 1
	}

	// hide unwanted areas in access tab of page
	TCEFORM.pages {
	    extendToSubpages.disabled = 1
	    fe_group.disabled = 1
	    fe_login_mode.disabled = 1
	}

	// hide unwanted areas in metadata tab of page
	TCEFORM.pages {
	    author_email.disabled = 1
	    lastUpdated.disabled = 1
	}

	// hide all areas of appearance tab of page
    TCEFORM.pages {
        layout.disabled = 1
        backend_layout.disabled = 1
        backend_layout_next_level.disabled = 1
        content_from_pid.disabled = 1
        newUntil.disabled = 1
    }

    // hide all areas of behaviour tab of page
    TCEFORM.pages {
        alias.disabled = 1
        url_scheme.disabled = 1
        cache_timeout.disabled = 1
        no_cache.disabled = 1
        cache_tags.disabled = 1
        is_siteroot.disabled = 1
        no_search.disabled = 1
        editlock.disabled = 1
        php_tree_stop.disabled = 1
        module.disabled = 1
        target.disabled = 1
    }

    // hide all areas of resources tab of page
    TCEFORM.pages {
        media.disabled = 1
        storage_pid.disabled = 1
        TSconfig.disabled = 1
        tsconfig_includes.disabled = 1
    }

    // hide all areas of categories tab of page
    TCEFORM.pages {
        categories.disabled = 1
    }

    // hide unwanted content elements in selection
	TCEFORM.tt_content.CType.removeItems = table,uploads,shortcut,div,html,mailform,login

	// hide unwanted areas in general tab of content elements
	TCEFORM.tt_content {
        colPos.disabled = 1
		date.disabled = 1
		header_link.disabled = 1
		rowDescription.disabled = 1
	}

	// hide unwanted areas in media tab of content elements
	setup.override.edit_docModuleUpload = 0

	// hide unwanted areas in appearance tab of content elements
    TCEFORM.tt_content {
		imageheight.disabled = 1
		imagewidth.disabled = 1
        imageborder.disabled = 1
		linkToTop.disabled = 1
    }

    // hide unwanted areas in access tab of content elements
    TCEFORM.tt_content {
        date.disabled = 1
        fe_group.disabled = 1
        starttime.disabled = 1
        endtime.disabled = 1
        editlock.disabled = 1
    }

    // hide categories tab of content elements
    TCEFORM.tt_content {
        categories.disabled = 1
    }

    // show ohly wanted aspect ratios for image manipulation
    // setting a default value only possible with typo3 8
    TCEFORM.sys_file_reference.crop.config.ratios {
      1\.7777777777777777= 16:9
    }
[end]
