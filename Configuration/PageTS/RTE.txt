################################################
#### RTE BASICS                             ####
################################################
// Configuration RTE

RTE.default {
toolbarOrder (
          formatblock,
          fontsize,
          textstyle,
          textstylelabel,
          blockstyle,
          blockstylelabel,
          linebreak,
          bold,
          italic,
          subscript,
          superscript,
          orderedlist,
          unorderedlist,
          link,
          image,
          findreplace,
          insertcharacter,
          indent,
          definitionlist,
          removeformat,
          chMode
    )

    hideButtons (
        textstylelabel,
        textstyle,
        tableproperties,
        tablerestyle,
        rowproperties,
        rowinsertabove,
        rowinsertunder,
        rowdelete,
        rowsplit,
        columnproperties,
        columninsertbefore,
        columninsertafter,
        columndelete,
        columnsplit,
        cellproperties,
        cellinsertbefore,
        cellinsertafter,
        celldelete,
        cellsplit,
        cellmerge,
        toggleborders,
        about
    )

    # Remove items in format list
    buttons.formatblock {
        removeItems (
            address,
            article,
            aside,
            blockquote,
            div,
            footer,
            nav,
            header,
            p,
            h1,
            h5,
            h6,
            pre,
            section
        )
    }

    # allow definition lists
    showButtons := addToList(definitionlist)
    proc.allowTags := addToList(dl,dt,dd)

    # add custom block style for literature lists
    proc.allowedClasses >
    proc.allowedClasses = literature
    buttons.blockstyle.tags.p.allowedClasses = literature
    contentCSS >
    contentCSS = typo3conf/ext/tmpl_fidaac/Resources/Public/Css/rte.css
    showTagFreeClasses = 0
    enableWordClean = 1
    useCSS = 0
}

RTE.classes >
RTE.classes {
    literature {
        name = Literature
        value = literature
    }
}

RTE.default.FE < RTE.default
RTE.default.FE.FE >

RTE.config.tt_content.bodytext.proc.allowedClasses = literature
